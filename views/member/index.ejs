<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    </title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body  >
    <div class=" mx-20 flex flex-col gap-4">
        <h2 class="text-4xl font-black p-4">User Profile</h2>
        <div class="flex gap-3">
            <label class="block text-gray-700 font-bold mb-2">Name:</label>
            <p class="text-gray-900 font-medium"><%= user.name %></p>
        </div>
        <div class="flex gap-3">
            <label class="block text-gray-700 font-bold mb-2">Membername:</label>
            <p class="text-gray-900 font-medium"><%= user.membername %></p>
        </div>
        <div class="flex gap-3">
            <label class="block text-gray-700 font-bold mb-2">Year of Birth:</label>
            <p class="text-gray-900 font-medium"><%= user.YOB %></p>
        </div>
        <div class="flex gap-3">
            <label class="block text-gray-700 font-bold mb-2">Role:</label>
            <p class="text-gray-900 font-medium"><%= user.isAdmin ? "Admin" :"Member" %></p>
        </div>
        <div class="flex gap-4">
            <a href="/logout" class="py-2 px-4  border-gray-500  border-2  font-semibold rounded hover:bg-gray-200">Logout</a>
            <a href="/member/<%= user._id %>/update" class="py-2 px-4 bg-gray-800 text-white font-semibold rounded hover:bg-gray-700">Update</a>
        </div>
    </div>
</body>
</html>
