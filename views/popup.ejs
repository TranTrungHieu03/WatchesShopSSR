<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <script>
      $(document).ready(function () {
        $(".close-alert").on("click", function () {
          $(this).hide();
        });
      });
    </script>
  </head>

  <body>
    <div>
      <div>
        <% if(message?.type == "success"){ %>
        <div class="close-alert bg-blue-300 w-full py-4 text-center relative font-semibold">
          <span class="absolute right-0 cursor-pointer text-4xl px-2 top-2"
            >&times;</span
          >
          <%= message?.message %>
        </div>
        <% } %>
      </div>
      <div>
        <% if(message?.type == "danger"){ %>
        <div class="close-alert bg-red-300 w-full py-4 text-center relative font-semibold">
          <span class="absolute right-0 cursor-pointer text-4xl px-2 top-2"
            >&times;</span
          >
          <%= message?.message %>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
