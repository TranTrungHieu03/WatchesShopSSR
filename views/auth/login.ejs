<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <script>
      $(document).ready(function () {
        $(".close-alert").on("click", function () {
          $(this).hide();
        });
      });
    </script>
  </head>
  <body>
    <div class="flex flex-col justify-center items-center">
      <% if(message?.type == "danger"){ %>
      <div class="close-alert bg-red-300 w-full py-4 text-center relative font-semibold">
        <span class="absolute right-0 cursor-pointer text-4xl px-2 top-2"
          >&times;</span
        >
        <%= message?.message %>
      </div>
      <% } %> <% if(message?.type == "success"){ %>
      <div class="close-alert  bg-blue-300 w-full py-4 text-center relative font-semibold">
        <span class="absolute right-0 cursor-pointer text-4xl px-2 top-2"
          >&times;</span
        >
        <%= message?.message %>
      </div>
      <% } %>

      <div class="container mx-auto md:w-1/3 sm:w-2/3 border p-4 mt-10">
        <form action="/login" method="POST" class="flex flex-col gap-6">
          <div class="text-4xl font-extrabold text-center">Login Form</div>
          <% if (info) { %>
          <div class="flex flex-col gap-2">
            <div>
              <label for="membername" class="font-medium">Member name</label>
              <input
                type="text"
                name="membername"
                class="border-input placeholder:text-muted-foreground focus-visible:ring-ring flex h-9 w-full border bg-transparent px-3 py-1 text-sm shadow-sm transition-colors file:border-0 file:bg-transparent file:text-sm file:font-medium focus-visible:outline-none focus-visible:ring-1 disabled:cursor-not-allowed disabled:opacity-50"
                id=""
                value="<%= info.membername %>"
                required
              />
            </div>
            <div>
              <label for="password">Password</label>
              <input
                type="password"
                name="password"
                class="border-input placeholder:text-muted-foreground focus-visible:ring-ring flex h-9 w-full border bg-transparent px-3 py-1 text-sm shadow-sm transition-colors file:border-0 file:bg-transparent file:text-sm file:font-medium focus-visible:outline-none focus-visible:ring-1 disabled:cursor-not-allowed disabled:opacity-50"
                id=""
                value="<%= info.password %>"
                required
                pattern="(?=.*[a-zA-Z0-9]).{6,}"
                title="Password must be at least 6 characters long ."
              />
            </div>
          </div>
          <% }else { %>
          <div class="flex flex-col gap-2">
            <div>
              <label for="membername" class="font-medium">Member name</label>
              <input
                type="text"
                name="membername"
                class="border-input placeholder:text-muted-foreground focus-visible:ring-ring flex h-9 w-full border bg-transparent px-3 py-1 text-sm shadow-sm transition-colors file:border-0 file:bg-transparent file:text-sm file:font-medium focus-visible:outline-none focus-visible:ring-1 disabled:cursor-not-allowed disabled:opacity-50"
                id=""
                required
              />
            </div>
            <div>
              <label for="password">Password</label>
              <input
                type="password"
                name="password"
                class="border-input placeholder:text-muted-foreground focus-visible:ring-ring flex h-9 w-full border bg-transparent px-3 py-1 text-sm shadow-sm transition-colors file:border-0 file:bg-transparent file:text-sm file:font-medium focus-visible:outline-none focus-visible:ring-1 disabled:cursor-not-allowed disabled:opacity-50"
                id=""
                required
                pattern="(?=.*[a-zA-Z0-9]).{6,}"
                title="Password must be at least 6 characters long ."
              />
            </div>
          </div>
          <%} %>
          <div class="">
            <div class="text-sm text-right font-medium">
              Have you had an account yet?
              <span class="font-bold hover-underline"
                ><a href="/signup">Signup</a></span
              >
            </div>
          </div>
          <button type="submit" class="bg-black py-2 px-3 text-white w-fit">
            Submit
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
